package{	import flash.events.Event;  import flash.display.*;  import flash.geom.Point;  import flash.utils.*;  [SWF(width="640", height="512",   backgroundColor="#FFFFFF", frameRate="60")]  	public class SpeedTest extends Sprite	{ 	   	private var _array:Array = [];		private var _dictionary:Dictionary = new Dictionary(true);				public function SpeedTest():void		{ 		  var start:int = getTimer();		  for(var i:int = 0; i < 100000; i++)      {        var movieClip:MovieClip = new MovieClip();        _array.push(movieClip);      }      var end:int = getTimer();      var difference:int = end - start;      trace(difference);            var object:MovieClip;      start = getTimer();      for(var j:int = 0; j < _array.length; j++)      {        object = _array[j];      }      end = getTimer();      difference = end - start;      trace(difference);            start = getTimer();      for(var k:int = 0; k < 100000; k++)      {        var movieClip2:MovieClip = new MovieClip();        _dictionary[movieClip2] = movieClip2;      }      end = getTimer();      difference = end - start;      trace(difference);            start = getTimer();      for each(var movieClip3:MovieClip in _dictionary)      {        object = _dictionary[movieClip3];      }      end = getTimer();      difference = end - start;      trace(difference);    }	}}