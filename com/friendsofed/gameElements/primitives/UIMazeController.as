package com.friendsofed.gameElements.primitives{	import flash.events.KeyboardEvent;	import flash.ui.Keyboard;	import flash.events.MouseEvent;	import flash.events.Event;	public class UIMazeController 	{	  private var _model:Object;		private var _stage:Object;		public function UIMazeController(model:AVerletModel):void 		{			_model = model;		}		internal function processKeyDown(event:KeyboardEvent):void		{		  if(event.keyCode == Keyboard.LEFT)		  {		    _model.direction = "left";		  }		  if(event.keyCode == Keyboard.RIGHT)		  {		    _model.direction = "right";		  }		  if(event.keyCode == Keyboard.DOWN)		  {		    _model.direction = "down";		  }		  if(event.keyCode == Keyboard.UP)		  {		    _model.direction = "up";		  }		}	}}